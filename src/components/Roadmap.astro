---
const timeline = [
  {
    title: "Prospector Free",
    date: "Avril 2025",
    description: "Un agent gratuit connecté au registre français des entreprises pour simplifier votre prospection initiale. Je travaille actuellement sur l'intégration des données publiques et l'interface conversationnelle."
  },
  {
    title: "Prospector Pro",
    date: "Juin 2025",
    description: "Version complète avec qualification automatisée et campagnes d'emails personnalisés. Je teste déjà des prototypes sur ma propre prospection avec des résultats prometteurs."
  },
  {
    title: "Bob Alpha",
    date: "Août 2025",
    description: "Premier accès à mon expert IA en intégration de solutions. Je construis actuellement sa base de connaissances en veille technologique permanente."
  }
];
---

<section class="section  bg-gradient-to-br from-secondary-200 dark:from-secondary-950/50 via-primary-200 dark:via-secondary-900/50 to-secondary-200 dark:to-secondary-950/50 text-white">
  <div class="container-custom">
    <div class="text-center max-w-3xl mx-auto mb-16">
      <span class="inline-block px-4 py-1 rounded-full bg-primary-900 text-primary-300 font-medium text-sm mb-4">
        Ma feuille de route
      </span>
      <h2 class="mb-6 text-primary-950 dark:text-white">La roadmap : construisons ensemble</h2>
    </div>
    
    <div class="max-w-3xl mx-auto">
      <div class="relative">
        <!-- Ligne verticale centrale -->
        <div class="absolute left-1/2 transform -translate-x-1/2 h-full w-0.5 bg-purple-700"></div>
        
        {timeline.map((item, index) => (
          <div class={`relative flex items-center mb-16 slide-up ${index % 2 === 0 ? 'justify-start' : 'justify-end'}`} style={`animation-delay: ${index * 150}ms`}>
            <!-- Contenu de la timeline (alterne gauche/droite) -->
            <div class={`w-full md:w-1/2 ${index % 2 === 0 ? 'pr-12 text-right' : 'pl-12 text-left'}`}>
              <div class="card p-6 border border-gray-700 bg-white">
                <div class={`flex ${index % 2 === 0 ? 'justify-end' : 'justify-start'} mb-4`}>
                  <span class="text-sm font-medium px-3 py-1 rounded-full bg-purple-900 text-purple-300">
                    {item.date}
                  </span>
                </div>
                <h3 class="text-xl font-semibold mb-3 text-primary-950 dark:text-white">{item.title}</h3>
                <p class="text-gray-900 dark:text-gray-100">{item.description}</p>
              </div>
            </div>
            
            <!-- Point central sur la timeline -->
            <div class="absolute left-1/2 top-6 transform -translate-x-1/2 w-8 h-8 rounded-full border-4 border-purple-700 bg-purple-500"></div>
          </div>
        ))}
      </div>
      
      <div class="text-center mt-12">
        <p class="text-gray-900 dark:text-gray-100 mb-8">
          Suivez mon avancée quotidienne sur le blog et contribuez à façonner ces outils selon vos besoins.
        </p>
        <a href="/blog" class="btn-primary">
          Découvrir le blog
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>